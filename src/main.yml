---

- include_tasks:
    file: "{{ item }}"
    apply:
      tags: preflight
  tags: components
  with_items:
    - src/local.yml
    - src/swapOff.yml
    #- src/vagrant.yml
    #- src/docker.yml
    #- src/kubernetes.yml
